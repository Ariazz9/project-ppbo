package enums;

/**
 * Enum representing different game states
 * Demonstrates enumeration usage for state management
 */
public enum GameState {
    MENU,
    PLAYING,
    PAUSED,
    GAME_OVER,
    SETTINGS
}
